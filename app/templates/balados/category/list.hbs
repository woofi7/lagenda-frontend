<div class="row">
  <div class="col">
    <div class="row">
      <h3 class="col">{{category.name}}</h3>
    </div>
    <div class="row">
      {{#each category.balados as |balado|}}
        <EpisodeCard @episode={{balado}} />
      {{/each}}
    </div>
  </div>
</div>